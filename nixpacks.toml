[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = ["cd frontend && npm install && npm run build"]

[start]
cmd = "cd backend && node src/scripts/initDb.js && node src/index.js"
